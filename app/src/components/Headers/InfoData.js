import React from "react";
import theme from "../../styles/theme.styles";

const PageHeader = ({ text }) => {
  return <label className="text-xs text-black font-bold ">{text}</label>;
};

export default PageHeader;
